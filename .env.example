# Copy to `.env` and adjust as needed.
# DATABASE_URL is required; the rest are optional with defaults.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/asgard
APP_HOST=127.0.0.1
APP_PORT=8080
RUST_LOG=info
